# 引き継ぎ — アルノポケットの不具合直し（2026-08-23）

次に入る人へ。**まず「いま残っている仕事」だけ読めば動けます。**

---

## いま残っている仕事

**GAS を1回貼り替えてもらう。それだけ。**

利用者は一度デプロイ済みだが、そのあとに見つけた不具合の修正（下の「壊れた写真」）が
まだ本番に載っていない。載せる手順はこれ：

```bash
bash tools/deploy-gas.sh     # 検証台 → 送信 → いまのデプロイを新バージョンに → 確認まで
```

これは**利用者の手元で走らせてもらう**必要がある（理由は「この環境の制約」）。
走らせたあとの出力を貼ってもらえれば読み解ける。

貼り替えが効いたかだけ見たいときは：

```bash
bash tools/check-gas.sh      # Gemini はほとんど使わない
```

---

## いまの状態

- **フロント（GitHub Pages）… 公開済み。** `main` のルートから配信されている
  （`gh-pages` ブランチも Actions も無く、`.nojekyll` あり）
- **サーバー（GAS）… 1回は貼られたが、最新ではない**
- `main` と作業ブランチ `claude/arno-pocket-family-code-transfer-s46wfe` は同じ中身
  （作業ブランチのものは全部 `main` に入っている）。作業ツリーはクリーン
- 検証：`node tools/gas-harness.mjs gas/Code.gs`（65件）、
  `node tools/flow-harness.mjs`（16件）。どちらも全通過

---

## 何を直したのか（再調査しなくて済むように）

### ① 棚を「パチリ」が使えなかった ← 本命

利用者の申告は「写真で中身を確認するモードが効かない」。原因は2つ重なっていた。

1. **サーバーが、棚が空の家を「実在しない」と判定していた。**
   `knownHouse()` は data シートに行があるかで判定していたが、入れたばかりの端末は
   棚が空なので同期しても1行も書かれない。結果 `knownHouse` が永久に false になり、
   「先に同期を済ませてください」と言われ続ける。**すでに済ませているのに。**
   そして棚を埋めるための「パチリ」こそが、その最初の一歩だった（堂々巡り）。
   → `houses` 台帳シートを新設。**同期しに来た時点で載せる**（品目0件でも）。
     台帳より前からある家は data シートを見て救済し、台帳に書き写す。

2. **端末側が、家族コードが無いとカメラすら開かなかった。**
   → 無ければ黙って作る。`needsAI()` で `await` してはいけない
     （指で押した流れが切れると iPhone でカメラが開かない）。
     間に合わなかったぶんは `postAI` が `code:'unknown-house'` を見て一度だけ頼み直す。

### ② 招待リンクが別ブラウザに引き継がれない

LINE で招待リンクを開いたあと「他のブラウザで開く」を選ぶと家族コードが消える。
合言葉を `#` の後ろに置いていたうえ、起動直後に URL から消していたため、
LINE が外部ブラウザに渡す URL にはもう合言葉が入っていなかった。
→ `?join=` にして、アプリ内ブラウザのときだけ URL に載せたままにする。

### ③ 共有側で見つかったもの（申告になかったが実バグ）

- **削除が家族に伝わらないことがある。** `srv` が `Date.now()` で、同じミリ秒に
  2回書くと「前回の続き（`srv > since`）」から漏れる。→ `srv` が必ず増えるようにした
- **家族コードを1文字ずつ打つたびに本当に繋ぎに行っていた。** 途中の「頭だけのコード」で
  同期し、手元の中身をそこへ送っていた。→ 欄から離れたときに切り替える
- **1000行を越えると期限が壊れる。** 書式がシート作成時の1000行ぶんしか無く、
  `2026-09-01` が日付型に変換されて家族に届く。→ 書く前に書式を敷く

### ④ Gemini のモデルが落ちても止まらないようにした

利用者の要望：落ちたら次の頭のいいモデルへ、回数制限でも次へ。そのうえで無料枠で動くこと。

落ちる理由で扱いを分ける：

| 状況 | HTTP | 動き |
|---|---|---|
| そのモデル名がもう無い | 404 | 6時間休ませて下りる |
| 無料枠に無い／上限／混雑 | 403 / 429 / 503 | 5分休ませて下りる |
| **こちらの送り方が悪い** | 400（モデル名を咎めていない） | **休ませず・下りず**、理由をそのまま返す |
| 鍵そのものが違う | API_KEY_INVALID 等 | 止めて「鍵が正しくありません」 |

400 の扱いは**あとから直した穴**。当初モデルのせいにして休ませていたので、
誰かが壊れた写真を1枚送ると上位モデルが5分間、家族ぜんぶに対して止まる作りだった。

候補（`gas/Code.gs` の `GEMINI_MODELS`、頭のいい順）：

```
gemini-3.7-flash        2026-08-13 GA。無料枠の有無は情報が割れている
gemini-3.6-flash        2026-07-21 GA。同上
gemini-3.5-flash        2026-07-16 時点で無料枠の行あり
gemini-3.1-flash-lite   無料枠の行が確認できているもの
gemini-2.5-flash        無料枠あり（10 RPM / 250 RPD）※2026-10-16 終了予定
gemini-2.5-flash-lite   無料枠あり（15 RPM / 1000 RPD）※同上。最後の砦
```

上が無料で使えるならそれが当たり、駄目なら下りて必ず答えが返る。
**どちらに転んでも動くので、無料枠の有無を決め打ちしていない。**
スクリプト プロパティ `GEMINI_MODELS`（カンマ区切り）でコードを触らずに差し替えられる。

**削除済みで、戻してはいけない名前**：
`gemini-flash-latest`（別名が廃止済み・404）、
`gemini-2.0-flash` / `gemini-2.0-flash-lite`（2026-06-01 停止済み）。
Pro 系も入れない（2026年4月に無料枠から外れている）。

### ⑤ ついでに塞いだもの

- Gemini に一度も届かなかったぶんは、1日の回数から戻す。
  混んでいた時間帯に数回試しただけでその日が終わらないように。
  戻すのは「一度も到達しなかった場合」だけ（壊れた画像を送りつけて戻させる、は効かない）
- 家ごとの上限（60/日）に加えて全体の上限（600/日）。
  家の登録を緩めたぶんの守りを補うため

---

## この環境の制約（次の人も同じはず）

- **`script.google.com` が塞がれている。** org のイーグレスポリシーで CONNECT が 403。
  `/root/.ccr/README.md` に「迂回せず報告せよ」とある。**迂回しないこと。**
  つまり GAS のデプロイも、動いている実物への確認も、こちらからはできない
- **Remote Control が繋がっていない。** `ListAgents` が空なので、
  利用者のマシン側のセッションにも渡せない。繋いでもらえれば渡せる
- `ai.google.dev` も WebFetch では塞がれている（WebSearch は通る）
- Google Fonts も塞がれている。ブラウザ確認で
  `ERR_CONNECTION_RESET` が1件出るが、**アプリの不具合ではない**

---

## 確かめていないこと（引き継ぎたい宿題）

1. **無料枠で 3.7 / 3.6 flash が本当に通るか。**
   公式ページを読めず、第三者情報が互いに矛盾していた。
   決め打ちせず「駄目なら下りる」構造にしてあるので動きはするが、
   実際に何が通ったかは、貼り替え後のログか `check-gas.sh` の出力で分かる
2. **LINE の実機で、招待リンクの引き継ぎが効くか。**
   Chromium では確認済み（`?join=` の付与・剥がし、旧 `#join=` の互換、
   打ちかけコードを送らないこと）。実機の LINE は未確認
3. **`GEMINI_MODEL` スクリプト プロパティに古い値が残っていないか。**
   残っていると最優先で試されるので、無効な名前だと毎回1往復無駄になる。
   削除してもらうのが安全

---

## 検証台の使い方

```bash
node tools/gas-harness.mjs gas/Code.gs   # サーバー側だけ（65件）
node tools/flow-harness.mjs              # 端末側と通し（16件）
bash tools/check-gas.sh                  # 動いている実物に向けて
```

`tools/flow-harness.mjs` は今回作ったもの。`js/sync.js` と `gas/Code.gs` を
`fetch` でつないで、実物のコードで通しの流れを走らせる
（入れたばかりの端末がパチリする、家族2台が同じ中身になる、削除が伝わる、など）。

`tools/check-gas.sh` そのものも、`gas/Code.gs` を HTTP で喋らせた偽サーバーに向けて、
**貼り替え済み・未貼り替えの両方を正しく見分けること**を確認してある。
新旧の見分けは、一度も同期していない家に `recognize` を投げて
`code:"unknown-house"` が返るかで判定する（Gemini を使わない）。

---

## 触るときの注意

- **「新しいデプロイ」を作らない。** `/exec` の URL が変わり、
  家族全員の「設定 > 共有」が一斉に切れる。必ず既存デプロイの新バージョンで
  （`tools/deploy-gas.sh` はそうなっている）
- `needsAI()` の中で `await` しない。カメラが開かなくなる
- モデル名を足すときは、必ず現役か確かめる。死んだ名前は往復が1回増えるだけ損
