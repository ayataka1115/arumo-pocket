# macOS が勝手に作る隠しファイル
.DS_Store

# 配信用のコピー。
# GitHub Pages はこのフォルダの中身をそのまま配るので、複製を持つ必要がない。
# 実際、複製が古いまま気づかず放置される事故が起きたので、置き場所を1つにする。
公開用/

# clasp（デプロイ用）。scriptId と Google の認証はコミットしない
.clasp.json
.clasprc.json
